# Local dev — direct browser fallback (optional if using vercel dev)
VITE_ANTHROPIC_KEY=sk-ant-api03-xxx

# Vercel production — use this name (server-side, never in JS bundle)
ANTHROPIC_API_KEY=sk-ant-api03-xxx

VITE_OPENCLAW_URL=http://67.205.162.212:18789
